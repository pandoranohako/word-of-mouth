=render 'shared/selectbox'
=render 'shared/display_btn'
- @post.each do |post|
  -if @school.id == post.school_id
    .post-contents__main--header
      .post-contents__main--header-age
        =link_to "aaaa",user_path(post.user)
        = "年齢:#{post.user.age}"
        = "性別:#{post.user.gender}"
        = "職業:#{post.user.carrier}" 
